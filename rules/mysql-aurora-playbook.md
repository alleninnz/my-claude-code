---
paths:
  - "**/*.sql"
  - "**/schema/**"
  - "**/migrations/**"
  - "**/migrate/**"
---

# MySQL Aurora Playbook

**YOU MUST invoke the `my-claude-code:mysql-aurora-playbook` skill via the Skill tool before proceeding** when working with any of the following: MySQL queries, Aurora schemas, index optimization, slow query troubleshooting, deadlock fixing, schema migrations, or Aurora connection pool/failover tuning. Do not write SQL or modify schemas without consulting the playbook first.
